# AURA Final-Format Resolution Coordinator

Branch: `final-format-resolution`

This checklist tracks the failed finalization items as bounded lanes. Each lane
must end as `DONE`, `REJECTED WITH EVIDENCE`, or
`BLOCKED BY SPECIFIC FORMAT ISSUE`.

| Lane | Owner | Failed item | Files/functions to inspect | Hypothesis | Implementation plan | Test plan | Benchmark plan | Commit subject | Final status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A | Coordinator + zstd explorer | fair Aura0-vs-zstd production benchmark | `src/bin/aura_bench.rs` zstd/transcode ops | Existing zstd baseline compares extra or mismatched work; product benchmark needs Aura1 bytes output on both sides | Added fair `aura0-to-aura1-bytes*` and `zstd-aura1-to-aura1-bytes*` ops with exact same in-memory Aura1 byte sink and verification metadata | `cargo test --test aura_bench_cli -- --nocapture`; `cargo test`; release build | `/tmp/aura-benchmarks/final-resolution-lane-a/*.json`; Aura0 loses to zstd on huff/nohuff production bytes | Add fair Aura0 vs zstd Aura1 expansion benchmark | DONE; AURA0 VS ZSTD FINAL TARGET FAILED |
| B | Coordinator + cursor explorer | `.aura0 -> .aura1` cursor path | `src/body.rs`, `src/generic_planner.rs`, `src/records.rs`, `src/bin/aura_bench.rs` | Cursor can remove stream vectors on supported plans but may be slower than bulk decode/materialized writer | Added `--decode-path cursor` supported-subset path using streaming Aura1 body writer; rejected as default because huff cursor 159.732 ms vs materialized 69.350 ms, nohuff cursor 149.055 ms vs materialized/auto 119.459 ms | unit unsupported-plan safe decline; huff/nohuff verify row/hash/guard equality | `/tmp/aura-benchmarks/final-resolution-lane-b/*.json` | Reject cursor Aura0 decode path with benchmark evidence | REJECTED WITH EVIDENCE |
| C | Coordinator + plan explorer | CompiledAuraPlan fully wired | `src/program.rs`, `src/records.rs`, `src/bin/aura_bench.rs`, transcode call sites | Direct paths can carry plan metadata consistently; reference paths may be documented reference-only | `parse-aura1`, direct/profiled transcodes, and Aura1 replay/scan emit plan hash; materialized paths documented as reference-only | compiled plan usage tests; full cargo tests | `/tmp/aura-benchmarks/final-resolution-lane-c/*.json` all checked final-candidate paths emit `compiled_plan_used=true` | Wire CompiledAuraPlan into final transcode and replay paths | DONE |
| D | Coordinator + encoder explorer | `.aura1 -> .aura0` column-free path | `src/generic_planner.rs::encode_generic_i64_columns_with_plan*`, `src/bin/aura_bench.rs` | Huffman/frequency planning does not require full columns in principle, but current encoder API does | Added explicit `--encoder-path column-free` rejection before Aura1 column materialization; current API requires Aura1 column buffers for dictionary/Huffman stream construction | CLI rejection test; direct-stream correctness tests remain | huff/nohuff materialized/direct/direct-streams benchmark evidence; column-free rejected by code-level blocker | Reject column-free Aura1 encoder with benchmark evidence | REJECTED WITH EVIDENCE |
| E | Coordinator + fixture explorer | compatible fixture coverage | `tests/aura_bench_cli.rs`, fixture helpers, benchmark fixture dirs | Unsupported external fixtures can be replaced by generated current-format fixtures except current public planner cannot generate Huffman-selected fixtures under the 2x speed gate | Added `aura-fixture-gen` metadata generator for tiny, dense/few-symbol, sparse/many-symbol, nohuff, and larger; huff metadata is an explicit format blocker entry | `cargo test --test fixture_generation -- --nocapture`; generated fixture decode roundtrips | `/tmp/aura-benchmarks/final-resolution-fixtures/fixtures.json`; huff remains external grimoire artifact | Add compatible benchmark fixture generation and metadata | BLOCKED BY SPECIFIC FORMAT ISSUE for generated huff; other fixtures DONE |
| F | Coordinator | final defaults/docs | `docs/FORMAT.md`, `docs/FOOTER.md`, `docs/COMPATIBILITY.md`, `docs/BENCHMARKING.md` | Defaults must be conservative unless lanes prove faster paths | Documented conservative defaults: Aura0 decode materialized/profiled, Aura1 encode direct materialized encoder, Aura1 fixed scan replay, no_guard production, canonical hash none by default; unresolved zstd/huff/cursor/column-free blockers remain explicit | doc consistency plus full verification | summarize final JSON evidence | Document final candidate defaults and unresolved format blockers | DONE |
